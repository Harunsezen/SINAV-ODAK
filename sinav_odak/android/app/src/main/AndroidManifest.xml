<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- FAZ 4 — Reklam kimliği.
         Play, google_mobile_ads kullanan uygulamalarda bu izni ve Data
         Safety beyanını ZORUNLU kılıyor. -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID"/>

    <!-- FAZ 6 — Bildirim ve zamanlama izinleri.

         Sayaç Timer tabanlı DEĞİL: blok/mola bitişleri mutlak zamana
         kurulmuş bildirimlerle haber verilir. Bu yüzden TAM ZAMANLI alarm
         gerekiyor; "inexact" alarm blok bitişini dakikalarca kaydırırdı.

         USE_EXACT_ALARM Play'de timer/alarm uygulamaları için izinli ama
         Console'da gerekçe yazılması gerekiyor.

         RECEIVE_BOOT_COMPLETED: cihaz yeniden başlarsa OS'a teslim edilmiş
         bildirimler silinir; boot receiver onları yeniden kurar. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.VIBRATE"/>

    <application
        android:label="Sınav Odak"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- FAZ 4 — AdMob uygulama kimliği.
             ZORUNLU: google_mobile_ads bağımlılığı aktifken bu meta-data
             yoksa uygulama AÇILIŞTA ÇÖKER (SDK ContentProvider'da kontrol
             ediyor, çalışma zamanına kadar beklemiyor).

             Değer `manifestPlaceholders` üzerinden geliyor
             (android/app/build.gradle). Varsayılan GOOGLE TEST kimliği;
             production kimliği `-Padmob_app_id=...` veya
             gradle.properties ile geçilir ve koda ASLA girmez.

             Kendi reklamına ASLA tıklama — hesap kapanır. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="${admobAppId}"/>

        <!-- FAZ 6 — Yeniden başlatma sonrası bildirim kurulumu.
             Cihaz kapanıp açıldığında OS'a teslim edilmiş zamanlanmış
             bildirimler silinir; bu receiver onları geri yükler.
             Olmadan: gece telefonu kapatan kullanıcı sabahki mola
             bildirimini hiç almazdı. -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"/>

    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
