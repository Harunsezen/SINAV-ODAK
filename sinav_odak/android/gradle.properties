org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=2G -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true

# --- AdMob uygulama kimligi ---
#
# Bu deger manifest'e `manifestPlaceholders` ile giriyor
# (android/app/build.gradle). Yanlis veya bos olursa uygulama ACILISTA
# COKUYOR: google_mobile_ads SDK'si kimligi bir ContentProvider icinde,
# Dart kodu hic calismadan once kontrol ediyor.
#
# NEDEN ARTIK BURADA, KOMUT SATIRINDA DEGIL:
# 1.5.2 derlemesinde komut satirindaki `-Padmob_app_id=$APP_ID` kabuk
# tarafindan acilmadi; Gradle'a `$APP_ID` METNI gitti ve manifest'e
# `android:value="$APP_ID"` yazildi. Uygulama acilamadi, Play "Bozuk
# Islevsellik Politikasi" ile reddetti. Kabugun degisken acmasina bagli
# olan bu zincir artik yok.
#
# GIZLI DEGIL: uygulama kimligi yayinlanan her APK'nin manifest'inde
# acikca duruyor, sir olmasi mumkun degil. Asil gizli tutulan sey
# keystore; o `key.properties`te ve depoya hic girmiyor.
#
# REKLAM BIRIMI kimlikleri yine komut satirinda (`--dart-define`):
# onlar unutulursa Google TEST birimlerine dusuluyor, yani hata GUVENLI
# tarafa dusuyor. Uygulama kimligi ise unutulursa COKUYOR - bu yuzden
# ikisi ayri yerde duruyor.
admob_app_id=ca-app-pub-6172662947666489~1593328391
